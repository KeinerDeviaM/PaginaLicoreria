import React from 'react';
import { Link } from 'react-router-dom';

export default function HomePage() {
  return (
    <div className="page">
      <section
        className="card reveal-on-scroll hero"
        style={{
          marginBottom: 20,
          padding: 32,
          minHeight: 420,
          display: 'grid',
          alignItems: 'center',
          background: `
            radial-gradient(circle at top left, rgba(212,175,55,0.14), transparent 22%),
            radial-gradient(circle at right center, rgba(255,255,255,0.05), transparent 20%),
            linear-gradient(135deg, rgba(18,18,18,0.98), rgba(10,10,10,0.96))
          `
        }}
      >
        <div style={{ maxWidth: 760, position: 'relative', zIndex: 2 }}>
          <div
            className="small"
            style={{
              display: 'inline-block',
              padding: '8px 14px',
              borderRadius: 999,
              border: '1px solid rgba(212,175,55,0.28)',
              background: 'rgba(212,175,55,0.08)',
              color: '#d4af37',
              marginBottom: 14
            }}
          >
            Licorería premium · Compra fácil y segura
          </div>

          <h1
            style={{
              fontSize: 'clamp(2.3rem, 5vw, 4.6rem)',
              lineHeight: 1.05,
              marginBottom: 14,
              maxWidth: 820
            }}
          >
            Descubre tu próxima botella favorita con una experiencia elegante y moderna
          </h1>

          <p
            className="small"
            style={{
              fontSize: '1rem',
              lineHeight: 1.8,
              maxWidth: 700,
              marginBottom: 22
            }}
          >
            Explora whiskies, vinos, rones, vodkas, tequilas y cervezas en una tienda diseñada para destacar cada producto
            con una presentación premium, navegación clara y un proceso de compra más atractivo.
          </p>

          <div className="stack" style={{ gap: 12, flexWrap: 'wrap' }}>
            <Link to="/shop/products" className="btn btn-primary">Explorar catálogo</Link>
            <Link to="/cart" className="btn btn-outline">Ir al carrito</Link>
          </div>
        </div>
      </section>

      <div className="grid grid-3" style={{ marginBottom: 20 }}>
        <section className="card reveal-on-scroll stat-card">
          <div className="small" style={{ color: '#d4af37', marginBottom: 8 }}>Selección premium</div>
          <h3 style={{ marginBottom: 8 }}>Marcas destacadas</h3>
          <p className="small" style={{ lineHeight: 1.7 }}>
            Encuentra una selección visualmente cuidada de licores, vinos y cervezas con una presencia más moderna.
          </p>
        </section>

        <section className="card reveal-on-scroll stat-card">
          <div className="small" style={{ color: '#d4af37', marginBottom: 8 }}>Compra ágil</div>
          <h3 style={{ marginBottom: 8 }}>Proceso sencillo</h3>
          <p className="small" style={{ lineHeight: 1.7 }}>
            Agrega al carrito, revisa tu pedido y completa la compra con una interfaz más clara y agradable.
          </p>
        </section>

        <section className="card reveal-on-scroll stat-card">
          <div className="small" style={{ color: '#d4af37', marginBottom: 8 }}>Experiencia premium</div>
          <h3 style={{ marginBottom: 8 }}>Diseño con carácter</h3>
          <p className="small" style={{ lineHeight: 1.7 }}>
            Una estética oscura, elegante y atractiva pensada para resaltar el producto y transmitir más calidad.
          </p>
        </section>
      </div>

      <section className="card reveal-on-scroll" style={{ marginBottom: 20 }}>
        <div className="page-header" style={{ alignItems: 'center' }}>
          <div>
            <div className="small" style={{ color: '#d4af37', marginBottom: 8 }}>¿Por qué elegirnos?</div>
            <h2 style={{ marginBottom: 8 }}>Una tienda pensada para vender mejor</h2>
            <p className="small" style={{ maxWidth: 760, lineHeight: 1.8 }}>
              No es solo un catálogo. Es una experiencia donde cada producto gana más presencia y cada compra se siente más confiable.
            </p>
          </div>
        </div>

        <div className="grid grid-3" style={{ marginTop: 18 }}>
          <div className="card" style={{ background: 'rgba(255,255,255,0.02)' }}>
            <h3>Presentación visual</h3>
            <p className="small">Productos mejor exhibidos para captar la atención desde el primer vistazo.</p>
          </div>

          <div className="card" style={{ background: 'rgba(255,255,255,0.02)' }}>
            <h3>Mayor confianza</h3>
            <p className="small">Diseño más limpio, navegación clara y mejor lectura en todo el flujo de compra.</p>
          </div>

          <div className="card" style={{ background: 'rgba(255,255,255,0.02)' }}>
            <h3>Experiencia moderna</h3>
            <p className="small">Animaciones suaves, cards premium y mejor jerarquía visual en toda la interfaz.</p>
          </div>
        </div>
      </section>

      <section
        className="card reveal-on-scroll"
        style={{
          background: 'linear-gradient(135deg, rgba(212,175,55,0.08), rgba(255,255,255,0.02))',
          border: '1px solid rgba(212,175,55,0.18)'
        }}
      >
        <div className="stack" style={{ justifyContent: 'space-between', alignItems: 'center', gap: 16, flexWrap: 'wrap' }}>
          <div>
            <div className="small" style={{ color: '#d4af37', marginBottom: 8 }}>Empieza ahora</div>
            <h2 style={{ marginBottom: 8 }}>Explora nuestro catálogo completo</h2>
            <p className="small" style={{ lineHeight: 1.8 }}>
              Descubre botellas con mejor presencia visual y una experiencia de compra mucho más atractiva.
            </p>
          </div>

          <div className="stack" style={{ gap: 10, flexWrap: 'wrap' }}>
            <Link to="/shop/products" className="btn btn-primary">Ver productos</Link>
            <Link to="/profile" className="btn btn-outline">Mi perfil</Link>
          </div>
        </div>
      </section>
    </div>
  );
}
